# Environment Variables - Example Template
# Copy this to .env and fill in your actual values

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_mysql_password_here
DB_NAME=hardware_shop

# Server Configuration
PORT=3000
NODE_ENV=development

# Production Configuration (for deployment)
# When deploying to cloud:
# 1. Create these variables in your hosting platform dashboard
# 2. Use the database URL provided by your host
# 3. Set NODE_ENV=production

# Example for Railway/Heroku:
# DATABASE_URL=mysql://user:pass@host:port/database
