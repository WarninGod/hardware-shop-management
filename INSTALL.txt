🎯 HARDWARE SHOP MANAGEMENT SYSTEM - INSTALLATION GUIDE
═══════════════════════════════════════════════════════════════════════════════

Your complete application has been created! Follow these steps to get it running:

═══════════════════════════════════════════════════════════════════════════════
⚠️  PREREQUISITES - Check Before Starting
═══════════════════════════════════════════════════════════════════════════════

1. Node.js (v14 or higher)
   Download: https://nodejs.org
   Check: Open Command Prompt, run: node --version
   Expected: v14.0.0 or higher

2. MySQL Server (v5.7 or higher)
   Download: https://www.mysql.com/downloads/mysql/
   Check: Run: mysql --version
   Expected: mysql  Ver 5.7+ or 8.0+

3. MySQL is Running
   - Check if MySQL service is running
   - Windows: Services → MySQL80 (or version number)
   - You should be able to: mysql -u root -p (will ask for password)

═══════════════════════════════════════════════════════════════════════════════
📝 STEP-BY-STEP INSTALLATION
═══════════════════════════════════════════════════════════════════════════════

STEP 1: Open Command Prompt
────────────────────────────
- Press: Windows Key + R
- Type: cmd
- Press: Enter
- Navigate to project folder:
  cd c:\Users\FSOS\Documents\Delhi47Project

STEP 2: Edit Configuration (.env file)
──────────────────────────────────────
- This file contains your database credentials
- Find: .env in project root
- Open with Notepad

Current content:
  DB_HOST=localhost
  DB_USER=root
  DB_PASSWORD=root          ← CHANGE THIS TO YOUR MYSQL PASSWORD!
  DB_NAME=hardware_shop
  PORT=3000
  NODE_ENV=development

ACTION: Update DB_PASSWORD with your actual MySQL root password
        (The password you set when installing MySQL)

Save the file (Ctrl+S)

STEP 3: Install Node.js Dependencies
─────────────────────────────────────
In Command Prompt, run:

  npm install

This will:
- Download Express, MySQL driver, CORS, and other dependencies
- Create a node_modules folder
- Take 2-3 minutes

Wait for it to complete without errors.

STEP 4: Create MySQL Database
──────────────────────────────
In Command Prompt, run:

  mysql -u root -p hardware_shop < server/schema.sql

This will:
- Create the hardware_shop database
- Create 3 tables: vendors, products, sales
- Set up all indexes and constraints
- Ask for password: Enter your MySQL password

Expected output: No errors, completes quietly

Note (Windows PowerShell users): The '<' redirection operator is not supported in PowerShell.
Use one of these alternatives from the project folder (C:\Users\FSOS\Documents\Delhi47Project):

Option A — run via CMD (recommended):
  cmd /c ""C:\Program Files\MySQL\MySQL Server 9.5\bin\mysql.exe" -u root -p hardware_shop < server\schema.sql"

Option B — use mysql "source":
  & "C:\Program Files\MySQL\MySQL Server 9.5\bin\mysql.exe" -u root -p --database hardware_shop -e "source C:/Users/FSOS/Documents/Delhi47Project/server/schema.sql"

Option C — pipe file contents:
  Get-Content -Raw "server\schema.sql" | & "C:\Program Files\MySQL\MySQL Server 9.5\bin\mysql.exe" -u root -p hardware_shop

STEP 5: (OPTIONAL) Load Sample Data
────────────────────────────────────
To populate with test data, run:

  mysql -u root -p hardware_shop < server/sample-data.sql

This adds:
- 5 sample vendors
- 17 sample products
- 10 sample sales
- Useful for testing the system

Skip this if you want to start fresh.

Note (Windows PowerShell users): Use one of these instead (run from the project folder):

Option A — run via CMD (recommended):
  cmd /c ""C:\Program Files\MySQL\MySQL Server 9.5\bin\mysql.exe" -u root -p hardware_shop < server\sample-data.sql"

Option B — use mysql "source":
  & "C:\Program Files\MySQL\MySQL Server 9.5\bin\mysql.exe" -u root -p --database hardware_shop -e "source C:/Users/FSOS/Documents/Delhi47Project/server/sample-data.sql"

Option C — pipe file contents:
  Get-Content -Raw "server\sample-data.sql" | & "C:\Program Files\MySQL\MySQL Server 9.5\bin\mysql.exe" -u root -p hardware_shop

STEP 6: Start the Server
────────────────────────
In Command Prompt, run:

  npm start

Expected output:
  ✓ Database initialized successfully
  ✓ Server running at http://localhost:3000
  ✓ Frontend: http://localhost:3000
  ✓ API endpoints available at http://localhost:3000/api/*

If you see any errors:
- Check database credentials in .env
- Check if MySQL is running
- Check if port 3000 is available

STEP 7: Access the Application
───────────────────────────────
Open your web browser and go to:

  http://localhost:3000

You should see the Hardware Shop Management System dashboard!

═══════════════════════════════════════════════════════════════════════════════
✅ VERIFY EVERYTHING IS WORKING
═══════════════════════════════════════════════════════════════════════════════

1. Dashboard Page
   - Should show: 4 stat cards (Sales, Revenue, Profit, Stock)
   - Should show: "No sales yet" message initially

2. Vendors Page
   - Click "Add Vendor"
   - Enter: Name = "Test Vendor", Phone = "9876543210"
   - Click "Save Vendor"
   - Should show success message

3. Products Page
   - Click "Add Product"
   - Enter:
     • Name: "Hammer"
     • Category: "Tools"
     • Vendor: "Test Vendor" (select from dropdown)
     • Cost Price: 50
     • Selling Price: 75
     • Stock: 100
   - Click "Save Product"
   - Should show success message

4. Sales Page
   - Click "New Sale"
   - Select Product: "Hammer"
   - Enter Quantity: 5
   - Should auto-show Price: 75, Total: 375
   - Click "Complete Sale"
   - Should show: "Sale recorded! Profit: ₹125"

5. Reports Page
   - Click on "Summary" tab
   - Should show: 1 sale, ₹375 revenue, ₹125 profit
   - Click "Product Profit" tab
   - Should show Hammer with profit info

If all above work → ✅ Your system is installed correctly!

═══════════════════════════════════════════════════════════════════════════════
🆘 TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════════════

PROBLEM: "Cannot connect to MySQL"
───────────────────────────────────
Solution:
1. Ensure MySQL service is running
2. Check password in .env file
3. Test: mysql -u root -p (enter your password)
4. If fails, reinstall MySQL or reset password

PROBLEM: "npm install fails"
────────────────────────────
Solution:
1. Delete folder: node_modules (if exists)
2. Delete file: package-lock.json (if exists)
3. Run: npm cache clean --force
4. Run: npm install again

PROBLEM: "Port 3000 is already in use"
──────────────────────────────────────
Solution 1: Kill process using port 3000
  For Windows:
    netstat -ano | findstr :3000        (Find PID)
    taskkill /PID <PID> /F             (Kill it)
  
Solution 2: Change port in .env
  Change: PORT=3000
  To: PORT=3001
  Then run npm start

PROBLEM: "Cannot GET /vendors" in browser
──────────────────────────────────────────
Solution:
1. Check if backend server is running (should see npm start output)
2. Check if MySQL database exists
3. Refresh browser (Ctrl+F5)
4. Check browser console (F12 → Console tab) for errors
5. Check .env credentials are correct

PROBLEM: Forms not submitting
──────────────────────────────
Solution:
1. Open browser DevTools (F12)
2. Go to Console tab
3. Look for red error messages
4. Check if server is running
5. Check if API endpoints are accessible

═══════════════════════════════════════════════════════════════════════════════
📖 FILE LOCATIONS
═══════════════════════════════════════════════════════════════════════════════

All files are in: c:\Users\FSOS\Documents\Delhi47Project

Frontend Files:
  - client/index.html     (Main page)
  - client/style.css      (Styling)
  - client/script.js      (JavaScript logic)

Backend Files:
  - server/server.js      (Express server)
  - server/db.js          (Database connection)
  - server/schema.sql     (Database tables)

Configuration:
  - .env                  (⚠️ Edit with your password!)
  - package.json          (Dependencies)

Documentation:
  - README.md             (Full guide)
  - QUICKREF.md           (Quick reference)
  - DELIVERY_SUMMARY.txt  (What's included)
  - INSTALL.txt           (This file!)

═══════════════════════════════════════════════════════════════════════════════
🎯 QUICK START (If everything is already set up)
═══════════════════════════════════════════════════════════════════════════════

Every time you want to use the system:

1. Open Command Prompt
2. Go to: cd c:\Users\FSOS\Documents\Delhi47Project
3. Run: npm start
4. Open browser: http://localhost:3000

To stop the server: Press Ctrl+C in Command Prompt

═══════════════════════════════════════════════════════════════════════════════
💾 BACKING UP YOUR DATA
═══════════════════════════════════════════════════════════════════════════════

To backup your database:

  mysqldump -u root -p hardware_shop > backup.sql

To restore from backup:

  mysql -u root -p hardware_shop < backup.sql

═══════════════════════════════════════════════════════════════════════════════
✨ YOU'RE ALL SET!
═══════════════════════════════════════════════════════════════════════════════

Your hardware shop management system is ready to use!

Features included:
  ✓ Vendor management
  ✓ Product management with stock tracking
  ✓ Sales recording with automatic profit calculation
  ✓ Real-time profit and loss reports
  ✓ Product and vendor analysis
  ✓ Daily sales summaries
  ✓ Responsive design (works on mobile, tablet, desktop)
  ✓ Clean, professional interface

Need help? Check:
  - README.md (full documentation)
  - QUICKREF.md (command reference)
  - Server console for error messages
  - Browser console (F12) for frontend errors

═══════════════════════════════════════════════════════════════════════════════
Happy managing! 🎉
═══════════════════════════════════════════════════════════════════════════════
