════════════════════════════════════════════════════════════════════════════════
         🚀 DEPLOYING TO GITHUB PAGES + BACKEND HOSTING 🚀
         Hardware Shop Management System - Deployment Guide
════════════════════════════════════════════════════════════════════════════════

⚠️  IMPORTANT: GitHub Pages only hosts static files (HTML/CSS/JS).
    Your backend (Node.js + MySQL) must be hosted separately.

This guide covers:
  1. Deploying frontend to GitHub Pages (FREE)
  2. Deploying backend to cloud platforms (FREE tiers available)
  3. Connecting frontend to backend

════════════════════════════════════════════════════════════════════════════════
📦 PART 1: DEPLOY FRONTEND TO GITHUB PAGES
════════════════════════════════════════════════════════════════════════════════

STEP 1: Create GitHub Repository
─────────────────────────────────
1. Go to: https://github.com/new
2. Repository name: hardware-shop-management
3. Description: Hardware Shop Management System
4. Set as: Public
5. Click: "Create repository"

STEP 2: Push Frontend to GitHub
────────────────────────────────
Open PowerShell in your project folder and run:

  cd C:\Users\FSOS\Documents\Delhi47Project

  # Initialize git (if not already done)
  git init
  
  # Create .gitignore
  @"
node_modules/
.env
*.log
backup.sql
"@ | Out-File -FilePath .gitignore -Encoding utf8

  # Add all files
  git add .
  git commit -m "Initial commit: Hardware Shop Management System"
  
  # Link to your GitHub repo (replace YOUR_USERNAME)
  git remote add origin https://github.com/YOUR_USERNAME/hardware-shop-management.git
  
  # Push to GitHub
  git branch -M main
  git push -u origin main

STEP 3: Enable GitHub Pages
────────────────────────────
1. Go to your repository on GitHub
2. Click: Settings → Pages (left sidebar)
3. Source: Deploy from a branch
4. Branch: main
5. Folder: /client
6. Click: Save

⏰ Wait 2-3 minutes for deployment to complete

Your frontend will be live at:
  https://YOUR_USERNAME.github.io/hardware-shop-management/

════════════════════════════════════════════════════════════════════════════════
🖥️  PART 2: DEPLOY BACKEND (Choose ONE Platform)
════════════════════════════════════════════════════════════════════════════════

OPTION A: Railway (Recommended - Easiest)
──────────────────────────────────────────
✓ Free tier: $5 credit/month
✓ Auto-deploy from GitHub
✓ Built-in MySQL database
✓ Custom domains

Steps:
1. Go to: https://railway.app
2. Sign up with GitHub
3. Click: "New Project" → "Deploy from GitHub repo"
4. Select: hardware-shop-management
5. Add MySQL:
   - Click "+ New" → Database → MySQL
   - Railway will auto-create DB and set env vars
6. Configure backend:
   - Click on your service
   - Settings → Environment Variables
   - Add these variables:
     DB_HOST=<provided by Railway>
     DB_USER=<provided by Railway>
     DB_PASSWORD=<provided by Railway>
     DB_NAME=<provided by Railway>
     PORT=3000
     NODE_ENV=production
7. Set Root Directory:
   - Settings → Root Directory: /server
8. Deploy!

Your backend will be at: https://your-app-name.railway.app


OPTION B: Render (100% Free Tier Available)
────────────────────────────────────────────
✓ Free tier: 750 hours/month (enough for 1 app)
✓ Auto-deploy from GitHub
✓ No credit card required

Steps:
1. Go to: https://render.com
2. Sign up with GitHub
3. Create Web Service:
   - Dashboard → New → Web Service
   - Connect your GitHub repo
   - Name: hardware-shop-backend
   - Environment: Node
   - Build Command: cd server && npm install
   - Start Command: cd server && node server.js
   - Plan: Free
4. Add PostgreSQL Database:
   - Dashboard → New → PostgreSQL
   - Name: hardware-shop-db
   - Plan: Free
   - Note: You'll need to convert schema.sql to PostgreSQL syntax
     (or use their MySQL add-on partner)
5. Environment Variables:
   - In your Web Service → Environment
   - Add all DB_ variables from Render's database info
6. Deploy!

Your backend will be at: https://hardware-shop-backend.onrender.com


OPTION C: Heroku (Free Tier Discontinued, Paid Only)
─────────────────────────────────────────────────────
Cost: ~$7/month for Eco Dynos + ClearDB MySQL

Steps:
1. Install Heroku CLI: https://devcenter.heroku.com/articles/heroku-cli
2. Login:
   heroku login
3. Create app:
   heroku create hardware-shop-backend
4. Add MySQL:
   heroku addons:create cleardb:ignite
5. Get DB credentials:
   heroku config:get CLEARDB_DATABASE_URL
6. Set environment variables:
   heroku config:set DB_HOST=<from URL>
   heroku config:set DB_USER=<from URL>
   heroku config:set DB_PASSWORD=<from URL>
   heroku config:set DB_NAME=<from URL>
   heroku config:set NODE_ENV=production
7. Deploy:
   git subtree push --prefix server heroku main

Your backend will be at: https://hardware-shop-backend.herokuapp.com


OPTION D: Self-Hosted VPS (DigitalOcean, Linode, AWS)
──────────────────────────────────────────────────────
Cost: $5-10/month
Complexity: High (requires server management)

Not recommended for beginners. Requires:
  - Linux server setup
  - Node.js installation
  - MySQL installation
  - Nginx/Apache configuration
  - SSL certificate setup
  - Process manager (PM2)

════════════════════════════════════════════════════════════════════════════════
🔗 PART 3: CONNECT FRONTEND TO BACKEND
════════════════════════════════════════════════════════════════════════════════

STEP 1: Update Frontend Configuration
──────────────────────────────────────
Edit: client/config.js

Change this line:
  window.API_BASE_URL = 'http://localhost:3000';

To your backend URL (example for Railway):
  window.API_BASE_URL = 'https://your-app-name.railway.app';

STEP 2: Enable CORS on Backend
───────────────────────────────
Your server.js already has CORS enabled, but verify the origin.

In server/server.js, the CORS is set to allow all origins:
  app.use(cors());

For production, you may want to restrict it:
  app.use(cors({
    origin: 'https://YOUR_USERNAME.github.io'
  }));

STEP 3: Push Changes to GitHub
───────────────────────────────
  cd C:\Users\FSOS\Documents\Delhi47Project
  git add client/config.js
  git commit -m "Update API URL for production"
  git push origin main

GitHub Pages will auto-deploy in 2-3 minutes.

STEP 4: Test Your Deployment
─────────────────────────────
1. Open: https://YOUR_USERNAME.github.io/hardware-shop-management/
2. Click: Vendors → Add Vendor
3. Create a test vendor
4. Check if it saves (success toast should appear)
5. Open browser DevTools (F12) → Console
   - Should see: "✓ Application initialized successfully"
   - No CORS or fetch errors

If you see errors:
  - Check config.js has correct backend URL
  - Check backend is running (visit backend URL directly)
  - Check CORS is enabled on backend
  - Check browser console for specific error messages

════════════════════════════════════════════════════════════════════════════════
📊 COST SUMMARY
════════════════════════════════════════════════════════════════════════════════

Frontend (GitHub Pages):    FREE ✓
Backend Options:
  - Railway:                 $5/month credit (usually sufficient)
  - Render:                  FREE (with limitations)
  - Heroku:                  ~$7/month (Eco Dynos)
  - DigitalOcean VPS:        $6/month (requires setup)

Recommended for Beginners: Railway or Render (free tier)

════════════════════════════════════════════════════════════════════════════════
🔧 PRODUCTION CHECKLIST
════════════════════════════════════════════════════════════════════════════════

Before going live:
  ☐ Change all passwords in .env
  ☐ Set NODE_ENV=production on backend
  ☐ Update client/config.js with production API URL
  ☐ Test all features on live site
  ☐ Set up database backups (via hosting provider)
  ☐ Configure CORS to only allow your GitHub Pages domain
  ☐ Monitor backend logs for errors
  ☐ Set up SSL (HTTPS) - most platforms do this automatically

Security:
  ☐ Never commit .env file to GitHub
  ☐ Use strong database passwords
  ☐ Review CORS settings
  ☐ Enable rate limiting on backend (optional, for later)

Performance:
  ☐ Backend should have at least 512MB RAM
  ☐ Database connection pooling is enabled (already done)
  ☐ Consider adding caching for reports (optional)

════════════════════════════════════════════════════════════════════════════════
🆘 TROUBLESHOOTING
════════════════════════════════════════════════════════════════════════════════

PROBLEM: Frontend shows "Failed to fetch"
──────────────────────────────────────────
Solutions:
1. Check client/config.js has correct backend URL (with https://)
2. Verify backend is running: visit backend URL in browser
3. Check CORS is enabled in server/server.js
4. Check browser console (F12) for specific error

PROBLEM: Backend not connecting to database
────────────────────────────────────────────
Solutions:
1. Verify all DB_ environment variables are set correctly
2. Check database is running (via hosting provider dashboard)
3. Ensure DATABASE_URL is parsed correctly
4. Check backend logs for connection errors

PROBLEM: GitHub Pages shows 404
────────────────────────────────
Solutions:
1. Wait 5 minutes after enabling Pages
2. Check Settings → Pages shows "Your site is live"
3. Verify folder is set to /client
4. Try hard refresh (Ctrl+Shift+R)
5. Check repository is Public

PROBLEM: "Mixed content" errors (HTTP vs HTTPS)
────────────────────────────────────────────────
Solutions:
1. Ensure backend URL uses https:// (not http://)
2. Most hosting platforms auto-provision SSL
3. If self-hosting, set up Let's Encrypt SSL certificate

════════════════════════════════════════════════════════════════════════════════
📚 ADDITIONAL RESOURCES
════════════════════════════════════════════════════════════════════════════════

Railway Docs:      https://docs.railway.app
Render Docs:       https://render.com/docs
GitHub Pages:      https://pages.github.com
CORS Explanation:  https://developer.mozilla.org/en-US/docs/Web/HTTP/CORS

════════════════════════════════════════════════════════════════════════════════
✅ SUMMARY
════════════════════════════════════════════════════════════════════════════════

1. Frontend → GitHub Pages (FREE)
2. Backend → Railway/Render (FREE tier or ~$5/month)
3. Update client/config.js with backend URL
4. Push to GitHub
5. Test live site

Total time: 30-60 minutes
Total cost: $0-5/month

════════════════════════════════════════════════════════════════════════════════
Good luck with your deployment! 🚀
════════════════════════════════════════════════════════════════════════════════
